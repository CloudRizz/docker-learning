events {}

http {

    upstream flask_app {
        server task-manager:5000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://flask_app;
        }
    }
}